1-
 int count=1;
      for(int i=1;i<nums.length;i++){
        if(nums[i-1]!=nums[i]){
            nums[count]=nums[i];
            count++;
        }
      }
      System.out.println(count);
      return count;

2-
 int count=0;
      for(int i=1;i<nums.length;i++){
        if(nums[i]!=nums[count])
            count++;
             nums[count]=nums[i];
      }
      return count+1;
