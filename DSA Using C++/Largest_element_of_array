#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    // Write your code here.
    // sort(arr.begin(),arr.end());

    int max=0;
    for(int i =0;i<n;i++){
        if(arr[i]>arr[max]){
            max=i;
        }
    }
    return arr[max];
}
